machine:
  node:
    version: 0.10.25
dependencies:
  pre:
    - bash ui-pre.sh
test:
  override:
    - bash ui-test.sh
    - cd ui && gulp jshint
    - cd ui && gulp test
    - cd ui && gulp protractor		


