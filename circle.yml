machine:
  node:
    version: 0.10.25
dependencies:
  pre:
    - npm install -g gulp
    - npm -g install bower
    - cd ui && npm install
    - cd ui && bower install
test:
  override:
    - cd ui && gulp test	
