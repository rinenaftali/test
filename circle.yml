machine:
  node:
    version: 0.10.25
dependencies:
  pre:
    - if  [[ $CIRCLE_BRANCH == master ]] || [[ $CIRCLE_BRANCH == ui-* ]] ; then 
       - bash ui-pre.sh
    - fi 
test:
  override:
    - if  [[ $CIRCLE_BRANCH == master ]] || [[ $CIRCLE_BRANCH == ui-* ]] ; then 
       - cd ui && gulp jshint 
       - cd ui && gulp jshint
       - cd ui && gulp protractor 
    - fi 

